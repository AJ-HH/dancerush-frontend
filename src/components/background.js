import React from "react";
import Box from "@mui/material/Box";

const Background = () => {
  return (
    <Box
      component="span" 
      sx={{
        minHeight: "1000px",
        height: "100%",
        width: "100%",
      }}
      
    />
  );
};

export default Background;
